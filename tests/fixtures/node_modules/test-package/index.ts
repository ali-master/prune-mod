/**
 * TypeScript version of test package
 */

export interface TestPackageConfig {
  greeting: string;
  version: string;
}

export function testFunction(): string {
  return 'Hello from test-package (TypeScript)';
}

export const config: TestPackageConfig = {
  greeting: 'Hello from test-package',
  version: '1.0.0'
};

export default {
  testFunction,
  config
};