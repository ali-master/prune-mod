.PHONY: test build clean install

# Default target
all: install build test

# Install dependencies
install:
	npm install

# Run tests
test:
	npm test

# Build the project
build:
	npm run build

# Clean generated files
clean:
	rm -rf dist/
	rm -rf coverage/

# Lint code
lint:
	npm run lint