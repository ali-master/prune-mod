# CoffeeScript test file
class TestPackage
  constructor: (@name = "test-package") ->
  
  greet: ->
    "Hello from #{@name} (CoffeeScript)"
  
  getVersion: ->
    "1.0.0"

module.exports = TestPackage