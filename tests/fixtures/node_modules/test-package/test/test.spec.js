const { testFunction } = require('../index');

describe('Test Package Specs', () => {
  test('testFunction returns expected string', () => {
    expect(testFunction()).toBe('Hello from test-package');
  });

  test('module exports are correct', () => {
    const pkg = require('../index');
    expect(pkg.testFunction).toBeDefined();
    expect(pkg.version).toBe('1.0.0');
  });
});