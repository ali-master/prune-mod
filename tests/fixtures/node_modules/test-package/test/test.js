const assert = require('assert');
const { testFunction } = require('../index');

describe('Test Package', function() {
  describe('#testFunction()', function() {
    it('should return correct greeting', function() {
      assert.strictEqual(testFunction(), 'Hello from test-package');
    });
  });
});